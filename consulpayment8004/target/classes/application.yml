server:
  port: 8004

spring:
  application:
    name: consul-payment-provider
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        hostname: 127.0.0.1
        service-name: ${spring.application.name}